// React Component to display Category Cards in the Category Component
import React from 'react';

const CategoryGrid = ({ name, description }) => {
  return <div>Card</div>;
};

export default CategoryGrid;
